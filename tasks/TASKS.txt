# AIChatDeck Tasks

Make PageCardLayoutMode and it makes
a segmented control to the right of [Open | Sealed]:
- Scroll (current)
- ScrollContained Replies - assistant message portion
is limited to 8 lines high but has scroll overflow
active.
- Summary - user message + first 6 lines of reply
- Summary Slide - 
- SlidePresentation - reorganizes the



## Keyboard Navigation
### Alt + Enter for "Send to New Page":
  - "Send to New Page [Alt/Opt u+ Symbol + Enter]"
### Other
- Alt + Left and Alt + Right arrows always move the
slides to the right and left in case the cursor is in
the text box.
- Down/Up arrow when mouse is moved over the slide
will scroll it.
- If on first slide, left arrow pressed twice
will bring the user to the main menu, which should
be navigable with up and down arrows and return key
to open up a group of chat slides.
- alt + m : main menu


## Chat Length
To the left of the textbox,
a tab control for response length 
[ Short | Small | Medium | Auto ]

Short = 1-2 sentences
Small = 1-2 paragraphs
Medium = 3-5 paragraph
Auto = determined by AI, current setting.

This will likely change what is sent as an 
instruction added to the system prompt but 
it could be API I am not sure. research
the openai api for chatting to determine.


## Chat Features

- Enable right-click and contextual menu.
Will act on highlighted text row an unhighlighted
word, or highlighted text.

- Markdown for clickable popover terms.
The AI includes terms in bold but is told
to surround them in a markdown tag that
indicates it is a term that could be clicked
on for the user to get a definition in
a popover or start a new slide.  It will have
an outline on hover to indicate it can be
clicked on.
- The user highlights text and one of the
popover options that appears is "open in new
chat"

- Searches for YouTube videos when option
checked and embeds them in the response.

- Tell AI to wrap questions at the end
with markdown tags so that AIChatDeck will
render them as buttons.


---


- Each chat page can have a route, /page.

- The width param in the header bar should have two modes: [Auto | Fixed].
It is "Fixed" right now.  Auto 
sets the width of a slide to the width of the slide container
minus 100pt on each side so that the user sees
the presence of a slide on both sides and can tell if it is
the currently the last or the first slide.  It will be a segm
control that

- We should have app routes.  For the main menu it would be / with 
there being two paths for the views


- Slide Pagination. It would normally be good to have two or three
columns but the issue is that the user is reading
them inside a scrolling frame, so the portions
that are multi-column would have to fit inside
the scrolling the frame's height, being slightly less.
perhaps, for a multi-column view of the slide's contents,
we might then have a heuristic procedure that, in response to
the height of the viewport and the set width of the slides, 
breaks the slide into underlying layout
sections so that if any sections placed into columns 
exceed the height of the slide's content container,
they are divided into portions for scrolling.
the text will flow row-major.  
It will also skip tables, as it does now, 
and account for other conditions.  when the user
resizes the browser or the slide width parameter,
we want this same procedure to be executed again.
Along the way what is important to note is that
right now at three columns the text will overflow to the right
and get clipped.  It could be that even with three
columns there can be a minimum width underneath which 
three columns turns into two and and then one
where two turns into one, because column width has
to matter.


## Chat Types

Features: 
- [ ] Revise and add to app UI from one slide to the next
  - [ ] Branch off to new chat deck inside chat


### Chat Slide Types

- [ ] During a chat, dropdown menu to the left of the chat box 
lets the user select chat slide type; the chat session
enters that mode for the next slide.

- [ ] **Media Search**
  - Finds media of chosen type with query term
 and lists them in a 3 x 5 column grid.


- [ ] **Brief Responses**
  - [ ] No more than a paragraph response
  - [ ] Config in upper right-hand corner allows >1 paragraph

- [ ] **Progressive / Iterative**
  - [ ] Output is built up and operated on each time.  Basis for
  different subcategories
  Example Categories: Regular, Progressive Specs, Progressive Research,
  Progressive Task

- [ ] **Progressive Specs**
  - [ ] Each slide lets the user add to and revise specs
  - [ ] Conventions (e.g. Progressive Specs, Progressive Research) change the instructions sent to the AI for formatting responses and style them accordingly
  - [ ] target: `.mmd` or `plant-uml-c4` — rendered diagram in a container with a tab for text editor

- [ ] **Progressive Research**
  - [ ] Each slide lets the user add to and revise research, one step at a time
  - [ ] Ask questions for web search to add to it
  - [ ] Allow going back in history and branching off or writing over the last slide

- [ ] **Progressive Task**
  - reflects agent activity in each slide, user sees output and
  then tells agent what to do next

- [ ] **Teaching** (AI Agent)
  - Teaches a requested subject.  Does research on the web.
  - Asks scoping questions in questionnaire format.
  - Produces in-page quizzes, collects information, presents information
  in graphs and charts with built-in d3.js + AI agents.
  - User gives feedback and it adjusts.
  - Produces worksheets for print-out.
  
- [ ] **Translation**
  - [ ] Controls for switching between source and translation
  - [ ] Side-by-side view and related layout options

- [ ] Checkbox to the left of textbox: **Questionnaire / Intake Before Task**
  - questionnaire / intake types dropdown has different questionnaire types
  - takes information from the user, initially or periodically,
  with agents, to carry out tasks on or with the next slide.

## Chat Session View: Kanban

- [ ] View entire chat session (Q&A of all slides) in kanban view

## Chat Slide View

- [ ] Configure chat slide layout 
with controls at upper right


## Chat Deck Groups

- [ ] Allow grouping of chat session groups
- [ ] Kanban view of a chat deck group


## Branching Tracks

- [ ] Break a chat session into multiple subquestions
- [ ] Progress a chat rightward or branch downward (multiple tracks for a single slide)
- [ ] Navigate up and down between tracks

## Mobile / Tablet

Make sure the main menu has a good SPA layout for mobile devices.  
